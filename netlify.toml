[build]
command = "npm run build"
publish = "dist/public"

[build.environment]
NODE_VERSION = "20"

# SPA fallback so client routes work when directly loaded
[[redirects]]
from = "/*"
to = "/index.html"
status = 200
